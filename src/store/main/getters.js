export const toggleDrawer = (state) => {
  return state.toggleDrawer;
};

export const loggedUser = (state) => {
  return state.loggedUser;
};

export const company = (state) => {
  return state.loggedUser.company;
};
