language: node_js
node_js: 10.4.1
branches:
  only:
  - dev
before_install:
  - npm i -g cypress
addons:
  apt:
    packages:
      - libgconf-2-4
cache:
  npm: true
  directories:
    - ~/.cache
install:
  - npm ci
script:
  - npm run test:e2e:ci